{
    "name": "FoodGuard API",
    "description": "Multi-tenant API",
    "repository": "git@github.com:FGMark/foodguard-api.git",
    "env": {
        "TOKEN_SECRET": {
            "generator": "secret",
            "required": true
        },
        "MONGODB_URI": {
            "description": "The DB to connect to",
            "value": "http://",
            "required": true
        }
    },
    "formation": {
        "web": {
            "quantity": 1,
            "size": "hobby"
        }
    }
}
